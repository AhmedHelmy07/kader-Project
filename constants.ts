
export const MAP_WIDTH = 50;
export const MAP_HEIGHT = 30;
export const CELL_SIZE = 20; // in pixels
export const SIMULATION_TICK_RATE = 100; // in milliseconds
export const WHEELCHAIR_SPEED = 1; // cells per tick
export const BATTERY_DEPLETION_RATE = 0.05; // percent per tick
